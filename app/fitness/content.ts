export const CONTENT_HTML = `<!-- ===== AMBIENT LIGHT BEAMS ===== -->
<div class="light-beams" aria-hidden="true">
  <div class="beam beam-1"></div>
  <div class="beam beam-2"></div>
  <div class="beam beam-3"></div>
</div>



<!-- ===== SCROLL PROGRESS TRAIL ===== -->
<div class="scroll-progress" aria-hidden="true">
  <div class="scroll-progress-fill">
    <div class="scroll-progress-glow"></div>
    <div class="scroll-progress-smoke"></div>
  </div>
</div>

<!-- ===== HERO ===== -->
<section class="hero">
  <div class="sblob sblob-a" style="width:400px;height:400px;top:-10%;right:-5%;opacity:0.45;"></div>
  <div class="sblob sblob-b" style="width:350px;height:350px;bottom:5%;left:-8%;opacity:0.50;"></div>
  <canvas class="ember-canvas" aria-hidden="true"></canvas>
  <div class="container">
    <div class="hero-grid">
      <div>
        <div class="eyebrow">The Body Pillar</div>
        <h1>Your body is the vehicle. It's time to <em>rebuild the engine.</em></h1>
        <p class="hero-sub">The physical rebuild is the foundation everything else is built on. <strong>Stronger body, sharper mind, better life</strong> &mdash; it all starts with how you train, eat, and recover. This is where the transformation begins.</p>
        <div class="hero-cta-row">
          <a href="https://calendly.com/unlockaxia/30min" target="_blank" class="btn">Book Your Call</a>
          <a href="#approach" class="btn-ghost">See the Approach</a>
        </div>
      </div>
      <div class="hero-image">
        <img src="/images/rylan-physique.jpg" alt="Rylan — the Body Pillar of RH Transformation">
      </div>
    </div>
  </div>
</section>

<div class="molten-divider" aria-hidden="true"><span class="molten-dot"></span><span class="molten-residue"></span></div>

<!-- ===== THE PROBLEM ===== -->
<section class="story" id="problem">
  <div class="sblob sblob-c" style="width:450px;height:450px;top:-15%;right:-10%;opacity:0.55;"></div>
  <div class="sblob sblob-a" style="width:380px;height:380px;bottom:-10%;left:-5%;opacity:0.50;"></div>
  <div class="sblob sblob-d" style="width:300px;height:300px;top:40%;right:60%;opacity:0.45;"></div>
  <div class="sblob sblob-b" style="width:260px;height:260px;top:15%;left:75%;opacity:0.45;"></div>
  <div class="container">
    <div class="story-grid">
      <div class="story-text reveal-left">
        <div class="section-label">The Problem</div>
        <h2 class="section-title">You've been training wrong. <em>Here's why.</em></h2>
        <p>You've downloaded the YouTube programs. You've started strong on Monday and ghosted the gym by Wednesday. You've tried the splits, the challenges, the 30-day fixes &mdash; and none of it stuck.</p>
        <p>That's not because you lack discipline. It's because <strong>you've been doing random acts of fitness with no structure, no feedback, and no one who knows your body.</strong> Starting and stopping creates negative momentum. Every restart costs more willpower than the last.</p>
        <p>Most men aren't undertrained. They're <strong>misdirected.</strong> They don't need another program. They need a system built for them &mdash; and someone who adjusts it in real time.</p>
      </div>
      <div class="reveal-right" style="display:flex;align-items:center;">
        <div class="story-text">
          <div class="pull">"Random effort creates random results. Structured effort creates transformation."</div>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="molten-divider" aria-hidden="true"><span class="molten-dot"></span><span class="molten-residue"></span></div>

<!-- ===== THE RH APPROACH ===== -->
<section id="approach">
  <div class="sblob sblob-b" style="width:420px;height:420px;top:-10%;left:-8%;opacity:0.55;"></div>
  <div class="sblob sblob-d" style="width:350px;height:350px;bottom:-5%;right:-6%;opacity:0.50;"></div>
  <div class="sblob sblob-a" style="width:280px;height:280px;top:50%;right:70%;opacity:0.45;"></div>
  <div class="sblob sblob-c" style="width:300px;height:300px;top:20%;right:80%;opacity:0.45;animation-delay:-10s;"></div>
  <div class="container">
    <div class="story-grid">
      <div class="story-text reveal-left">
        <div class="section-label">The Approach</div>
        <h2 class="section-title">Custom-built for <em>your body.</em> Not a template.</h2>
        <p>Rylan doesn't hand you a PDF and wish you luck. He builds your program from scratch &mdash; based on your body, your schedule, your weak points, and your goals.</p>
        <p>Every workout is designed around <strong>progressive overload</strong> &mdash; controlled reps to failure, aesthetic muscle targeting, and strategic volume that forces adaptation without breaking you down. The program evolves as you do.</p>
        <p>This isn't a cookie-cutter split pulled from a fitness influencer's highlight reel. <strong>It's a system adapted to YOUR body</strong> &mdash; your imbalances, your schedule constraints, your recovery capacity. Rylan reviews your form, adjusts your loads, and makes sure every rep counts.</p>
      </div>
      <div class="story-image reveal-right">
        <img src="/images/rylan-marathon.jpg" alt="Rylan — custom coaching approach" loading="lazy">
      </div>
    </div>
  </div>
</section>

<div class="molten-divider" aria-hidden="true"><span class="molten-dot"></span><span class="molten-residue"></span></div>

<!-- ===== NUTRITION ===== -->
<section class="story" id="nutrition">
  <div class="sblob sblob-a" style="width:400px;height:400px;top:-12%;right:-8%;opacity:0.50;"></div>
  <div class="sblob sblob-c" style="width:380px;height:380px;bottom:-8%;left:-6%;opacity:0.55;"></div>
  <div class="sblob sblob-b" style="width:300px;height:300px;top:30%;left:80%;opacity:0.45;"></div>
  <div class="sblob sblob-d" style="width:280px;height:280px;top:60%;right:75%;opacity:0.45;animation-delay:-20s;"></div>
  <div class="container">
    <div class="reveal">
      <div class="section-label">Nutrition</div>
      <h2 class="section-title">Fuel the machine. <em>Don't starve it.</em></h2>
      <div class="nutrition-body">
        <p>There are no crash diets here. No obsessive macro counting. No meal prep prisons where you eat the same sad chicken and rice six times a day. <strong>Rylan builds a nutrition framework that fits your real life</strong> &mdash; your schedule, your preferences, your budget.</p>
        <p>The philosophy is simple: <strong>food is fuel, not punishment.</strong> You learn to eat in a way that supports your training, your energy, and your recovery without turning every meal into a math problem.</p>
        <p>Consistency beats perfection. A sustainable approach you follow for 15 weeks will always outperform the perfect diet you abandon after 10 days. <strong>Real meals. Real flexibility. Real results.</strong></p>
      </div>
    </div>
  </div>
</section>

<div class="molten-divider" aria-hidden="true"><span class="molten-dot"></span><span class="molten-residue"></span></div>

<!-- ===== WEEKLY RHYTHM ===== -->
<section id="rhythm">
  <div class="sblob sblob-c" style="width:450px;height:450px;top:-12%;right:-8%;opacity:0.55;"></div>
  <div class="sblob sblob-a" style="width:380px;height:380px;bottom:-10%;left:-6%;opacity:0.50;"></div>
  <div class="sblob sblob-b" style="width:320px;height:320px;top:30%;left:80%;opacity:0.45;"></div>
  <div class="sblob sblob-d" style="width:280px;height:280px;top:60%;right:75%;opacity:0.45;animation-delay:-20s;"></div>
  <div class="sblob sblob-a" style="width:300px;height:300px;top:10%;left:65%;opacity:0.45;animation-delay:-30s;"></div>
  <div class="container">
    <div class="reveal">
      <div class="section-label">Your Week</div>
      <h2 class="section-title">What a week with <em>Rylan</em> actually looks like.</h2>
      <p class="section-lead">This isn't a set-it-and-forget-it program. Every week has structure, accountability, and direct coaching contact.</p>
    </div>

    <div class="method-grid reveal-group">
      <div class="pillar reveal">
        <div class="pillar-num">&mdash; Training &mdash;</div>
        <h3 class="pillar-title">Train</h3>
        <em class="pillar-italic">Move with purpose.</em>
        <ul>
          <li>Custom workout plan for the week</li>
          <li>Form video reviews with feedback</li>
          <li>Progression tracking &amp; load adjustments</li>
          <li>Recovery &amp; mobility programming</li>
        </ul>
      </div>
      <div class="pillar reveal">
        <div class="pillar-num">&mdash; Check-ins &mdash;</div>
        <h3 class="pillar-title">Track</h3>
        <em class="pillar-italic">Stay honest.</em>
        <ul>
          <li>Daily accountability check-ins</li>
          <li>Nutrition logging &amp; feedback</li>
          <li>Energy &amp; sleep tracking</li>
          <li>Weekly progress photos &amp; measurements</li>
        </ul>
      </div>
      <div class="pillar reveal">
        <div class="pillar-num">&mdash; Coaching &mdash;</div>
        <h3 class="pillar-title">Coach</h3>
        <em class="pillar-italic">Build with guidance.</em>
        <ul>
          <li>Weekly 1-on-1 call with Rylan</li>
          <li>Real-time program adjustments</li>
          <li>Resistance-point planning</li>
          <li>Mindset &amp; motivation alignment</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<div class="molten-divider" aria-hidden="true"><span class="molten-dot"></span><span class="molten-residue"></span></div>

<!-- ===== BENEFITS ===== -->
<section class="story" id="benefits">
  <div class="sblob sblob-b" style="width:420px;height:420px;top:-10%;left:-8%;opacity:0.50;"></div>
  <div class="sblob sblob-d" style="width:350px;height:350px;bottom:-5%;right:-6%;opacity:0.55;"></div>
  <div class="sblob sblob-a" style="width:280px;height:280px;top:50%;right:70%;opacity:0.45;"></div>
  <div class="sblob sblob-c" style="width:300px;height:300px;top:20%;right:80%;opacity:0.45;animation-delay:-10s;"></div>
  <div class="container">
    <div class="reveal">
      <div class="section-label">What You Gain</div>
      <h2 class="section-title">Every benefit of the <em>physical rebuild.</em></h2>
    </div>
    <div class="benefits-grid reveal">
      <div class="benefit-item">Body composition transformation you can see and feel</div>
      <div class="benefit-item">Strength and functional fitness that carries into real life</div>
      <div class="benefit-item">Energy levels that make 6am feel different</div>
      <div class="benefit-item">Sleep quality that actually recovers you</div>
      <div class="benefit-item">Confidence from physical capability, not just appearance</div>
      <div class="benefit-item">Discipline that transfers to career, relationships, everything</div>
      <div class="benefit-item">Understanding your body for life &mdash; not just 15 weeks</div>
      <div class="benefit-item">Recovery and injury prevention knowledge</div>
      <div class="benefit-item">Sustainable habits that outlast the program</div>
      <div class="benefit-item">Mental toughness built through physical challenge</div>
    </div>
  </div>
</section>

<div class="molten-divider" aria-hidden="true"><span class="molten-dot"></span><span class="molten-residue"></span></div>

<!-- ===== PHASE BREAKDOWN ===== -->
<section class="phases" id="phases">
  <div class="sblob sblob-a" style="width:500px;height:500px;top:-12%;right:-8%;opacity:0.55;"></div>
  <div class="sblob sblob-c" style="width:400px;height:400px;bottom:-10%;left:-6%;opacity:0.50;"></div>
  <div class="sblob sblob-b" style="width:320px;height:320px;top:30%;left:80%;opacity:0.45;"></div>
  <div class="sblob sblob-d" style="width:280px;height:280px;top:60%;right:75%;opacity:0.45;animation-delay:-20s;"></div>
  <div class="sblob sblob-a" style="width:350px;height:350px;top:10%;left:65%;opacity:0.45;animation-delay:-30s;"></div>
  <div class="container">
    <div class="reveal">
      <div class="section-label">The Arc</div>
      <h2 class="section-title">How the body <em>evolves</em> across 15 weeks.</h2>
      <p class="section-lead">Three phases. Each one builds on the last. By the end, you don't need the program &mdash; you are the program.</p>
    </div>

    <div class="phase-grid reveal-group">
      <div class="phase reveal">
        <div class="phase-dot"></div>
        <div class="phase-num">&mdash; Phase I &mdash;</div>
        <div class="phase-weeks">Weeks 1&ndash;5</div>
        <h3 class="phase-title">Foundation</h3>
        <em class="phase-tag">Strip it down.</em>
        <p class="phase-body">Assessment, baseline testing, movement patterns, habit installation, and nutrition foundation. The goal is consistency &mdash; not intensity. We find the leaks and plug them before we build.</p>
      </div>
      <div class="phase reveal">
        <div class="phase-dot"></div>
        <div class="phase-num">&mdash; Phase II &mdash;</div>
        <div class="phase-weeks">Weeks 6&ndash;12</div>
        <h3 class="phase-title">Forge</h3>
        <em class="phase-tag">Build it back.</em>
        <p class="phase-body">Progressive overload, volume increase, aesthetic targeting, nutrition dialed in. This is where the mirror starts changing. The body adapts, the confidence builds, and the work becomes its own reward.</p>
      </div>
      <div class="phase reveal">
        <div class="phase-dot"></div>
        <div class="phase-num">&mdash; Phase III &mdash;</div>
        <div class="phase-weeks">Weeks 13&ndash;15</div>
        <h3 class="phase-title">Freedom</h3>
        <em class="phase-tag">Run it yourself.</em>
        <p class="phase-body">Independent execution, self-programming introduction. Rylan steps back while you prove you can sustain it. You graduate into a man who understands his body for life.</p>
      </div>
    </div>
  </div>
</section>

<div class="molten-divider" aria-hidden="true"><span class="molten-dot"></span><span class="molten-residue"></span></div>

<!-- ===== FINAL CTA ===== -->
<section class="final" id="book">
  <div class="sblob sblob-b" style="width:500px;height:500px;top:-15%;left:-5%;opacity:0.38;"></div>
  <div class="sblob sblob-c" style="width:400px;height:400px;bottom:-10%;right:-8%;opacity:0.50;"></div>
  <div class="sblob sblob-a" style="width:300px;height:300px;top:30%;right:70%;opacity:0.45;"></div>
  <div class="container">
    <div class="final-inner">
      <div class="final-eyebrow">Start Building</div>
      <h2>The next 15 weeks happen <em>either way.</em></h2>
      <p>Your body is either building or decaying. There is no neutral. Book a call with Rylan and find out what's possible.</p>
      <a href="https://calendly.com/unlockaxia/30min" target="_blank" class="btn" data-booking>Book Your Call</a>
    </div>
  </div>
</section>`;

export const LEGACY_SCRIPT = `const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  // Scroll reveal with stagger support
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('in');
        revealObserver.unobserve(e.target);
      }
    });
  }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });

  document.querySelectorAll('.reveal, .reveal-left, .reveal-right').forEach(el => {
    revealObserver.observe(el);
  });

  // ===== EMBER PARTICLE FIELD =====
  if (!prefersReducedMotion) {
    const canvas = document.querySelector('.ember-canvas');
    const hero = document.querySelector('.hero');
    if (canvas && hero) {
      const ctx = canvas.getContext('2d');
      let particles = [];
      const PARTICLE_COUNT = 60;
      let isVisible = true;

      function resizeCanvas() {
        const rect = hero.getBoundingClientRect();
        canvas.width = rect.width;
        canvas.height = rect.height;
      }

      function createParticle(randomY) {
        const colors = [
          { r: 201, g: 122, b: 79 },
          { r: 212, g: 165, b: 116 },
          { r: 255, g: 136, b: 72 },
          { r: 156, g: 88, b: 50 },
        ];
        const color = colors[Math.floor(Math.random() * colors.length)];
        return {
          x: Math.random() * canvas.width,
          y: randomY ? Math.random() * canvas.height : canvas.height + Math.random() * 40,
          size: 1 + Math.random() * 3,
          speedY: 0.15 + Math.random() * 0.6,
          wobbleSpeed: 0.005 + Math.random() * 0.015,
          wobbleOffset: Math.random() * Math.PI * 2,
          opacity: 0.2 + Math.random() * 0.6,
          color: color,
          glow: Math.random() > 0.85,
          life: 0,
          maxLife: 200 + Math.random() * 400,
        };
      }

      function initParticles() {
        particles = [];
        for (let i = 0; i < PARTICLE_COUNT; i++) {
          particles.push(createParticle(true));
        }
      }

      function drawParticle(p) {
        const lifeRatio = p.life / p.maxLife;
        let alpha = p.opacity;
        if (lifeRatio < 0.1) alpha *= lifeRatio / 0.1;
        else if (lifeRatio > 0.8) alpha *= (1 - lifeRatio) / 0.2;

        if (p.glow) {
          const grad = ctx.createRadialGradient(p.x, p.y, 0, p.x, p.y, p.size * 4);
          grad.addColorStop(0, \`rgba(\${p.color.r}, \${p.color.g}, \${p.color.b}, \${alpha * 0.4})\`);
          grad.addColorStop(1, \`rgba(\${p.color.r}, \${p.color.g}, \${p.color.b}, 0)\`);
          ctx.fillStyle = grad;
          ctx.beginPath();
          ctx.arc(p.x, p.y, p.size * 4, 0, Math.PI * 2);
          ctx.fill();
        }

        ctx.fillStyle = \`rgba(\${p.color.r}, \${p.color.g}, \${p.color.b}, \${alpha})\`;
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
        ctx.fill();
      }

      function animate() {
        if (!isVisible) { requestAnimationFrame(animate); return; }
        ctx.clearRect(0, 0, canvas.width, canvas.height);

        for (let i = particles.length - 1; i >= 0; i--) {
          const p = particles[i];
          p.life++;
          p.y -= p.speedY;
          p.x += Math.sin(p.life * p.wobbleSpeed + p.wobbleOffset) * 0.4;

          if (p.life >= p.maxLife || p.y < -20) {
            particles[i] = createParticle(false);
          } else {
            drawParticle(p);
          }
        }
        requestAnimationFrame(animate);
      }

      const heroObserver = new IntersectionObserver((entries) => {
        isVisible = entries[0].isIntersecting;
      }, { threshold: 0 });
      heroObserver.observe(hero);

      resizeCanvas();
      initParticles();
      animate();
      window.addEventListener('resize', () => { resizeCanvas(); initParticles(); });
    }
  }

  // ===== SCROLL HANDLER + MILESTONES =====
  const progressFill = document.querySelector('.scroll-progress-fill');
  const progressBar = document.querySelector('.scroll-progress');
  const navEl = document.querySelector('nav');
  const heroImg = document.querySelector('.hero-image img');
  const winH = window.innerHeight;

  // Build milestone markers — matched to this page's sections
  const milestoneData = [
    { selector: '#problem', label: 'Problem' },
    { selector: '#approach', label: 'Approach' },
    { selector: '#nutrition', label: 'Nutrition' },
    { selector: '#rhythm', label: 'Weekly Rhythm' },
    { selector: '#benefits', label: 'Benefits' },
    { selector: '#phases', label: 'Phases' },
    { selector: '.final', label: 'Lock In' },
  ];

  // Get absolute top of element relative to document
  function getDocTop(el) {
    let top = 0;
    while (el) {
      top += el.offsetTop;
      el = el.offsetParent;
    }
    return top;
  }

  const milestones = [];
  function cacheMilestonePositions() {
    const docHeight = document.documentElement.scrollHeight - winH;
    const barHeight = progressBar ? progressBar.offsetHeight : 0;
    milestones.forEach(m => {
      const elTop = getDocTop(m.el);
      m.pct = Math.min(elTop / (docHeight || 1), 1);
      m.marker.style.top = \`\${m.pct * barHeight}px\`;
    });
  }

  if (progressBar) {
    milestoneData.forEach(m => {
      const el = document.querySelector(m.selector);
      if (!el) return;
      const marker = document.createElement('div');
      marker.className = 'scroll-milestone';
      marker.innerHTML = \`<span class="scroll-milestone-label">\${m.label}</span>\`;
      progressBar.appendChild(marker);
      milestones.push({ el, marker, pct: 0 });
    });
  }

  // rAF-throttled scroll
  let scrollTicking = false;
  window.addEventListener('scroll', () => {
    if (!scrollTicking) {
      scrollTicking = true;
      requestAnimationFrame(() => {
        const y = window.scrollY;
        const docHeight = document.documentElement.scrollHeight - winH;
        const scrollPct = Math.min(y / docHeight, 1);

        navEl.classList.toggle('scrolled', y > 80);

        if (heroImg && !prefersReducedMotion && y < winH) {
          heroImg.style.transform = \`translateY(\${y * 0.08}px)\`;
        }

        if (progressFill) {
          progressFill.style.height = \`\${scrollPct * 100}%\`;
        }

        milestones.forEach(m => {
          m.marker.classList.toggle('passed', scrollPct >= m.pct);
        });

        scrollTicking = false;
      });
    }
  }, { passive: true });

  // Cache positions after layout settles, and on resize
  requestAnimationFrame(cacheMilestonePositions);
  window.addEventListener('resize', cacheMilestonePositions);

  // ===== MOLTEN DIVIDER OBSERVER =====
  const moltenObserver = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('fired');
        moltenObserver.unobserve(e.target);
      }
    });
  }, { threshold: 0, rootMargin: '0px 0px -5% 0px' });

  document.querySelectorAll('.molten-divider').forEach(el => {
    moltenObserver.observe(el);
  });`;
